# DocSign App - Environment Configuration

# Security
SECRET_KEY=supersecretkey_change_me_in_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# SMTP Email Configuration (Optional - for sending signing links)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=noreply@docsign.app

# Application URLs
APP_URL=http://localhost:5173

# Database (for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/docsign
